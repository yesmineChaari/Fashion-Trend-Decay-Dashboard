"""The Streamlit dashboard's own package: cached data access and shared page chrome.

`app/streamlit_app.py` and the page modules under `app/views/` are thin
Streamlit entry points, same relationship as `scripts/*.py` to the rest of
`fashion_trends` — the actual logic worth unit testing lives here instead.
"""
